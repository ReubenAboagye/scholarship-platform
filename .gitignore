.env.local
.env
# Ignore all .env variants EXCEPT the example file
.env.*
!.env.local.example

# Node modules
node_modules/

# Build outputs
.next/
out/

# Dependencies
# pnpm-lock.yaml is intentionally committed so Netlify can reproduce exact installs
